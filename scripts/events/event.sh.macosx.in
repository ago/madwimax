#!/bin/sh
# Usage: event.sh <event> <if-name>

case "$1" in
if-create)
	/sbin/ifconfig "$2" up
	;;
if-up)
	/usr/sbin/ipconfig set "$2" DHCP
	;;
if-down)
	;;
if-release)
	/sbin/ifconfig "$2" down
	;;
*)
	echo "Usage: $0 { if-create | if-up | if-down | if-release }" >&2
	exit 3
	;;
esac

